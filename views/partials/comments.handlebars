<div class="comments">
  {{#each this}}
  <section class="bg-white rounded shadow-2xl p-6 border-2 max-w-fit  ">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  </br>
  {{/each}}
</div>
