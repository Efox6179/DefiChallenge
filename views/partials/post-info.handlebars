<div class="flex">
    <article class="post">
    <div class=" bg-white rounded shadow-2xl p-12 ">
    <div class="title">
        <div class="title-bar">
            <h3><a href="/group/posts/{{id}}" class="bold text-2xl">{{title}}</a></h3>
            <span class="text-sm italic">Posted by {{user.username}} on {{format_date created_at}}</span>
        </div>
        <p class="bold">{{post_text}}</p>
    </div>
    </div>
</article>
</div>