<article class="challenge">
    <div>
        <div>
            <h3><a href="/challenges/{{id}}">{{title}}</a></h3>
            <span>Created by {{user.username}} on {{format_date created_at}}</span>
        </div>
        <p>{{challenge_text}}</p>
        <p>{{due_date}}</p>
    </div>
</article>